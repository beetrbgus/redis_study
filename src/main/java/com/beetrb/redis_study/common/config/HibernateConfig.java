package com.beetrb.redis_study.common.config;

import org.springframework.context.annotation.Bean;
import org.springframework.context.annotation.Configuration;
import org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter;

/*@Configuration
public class HibernateConfig {

    private static final HibernateJpaVendorAdapter HIBERNATE_JPA_VENDOR_ADAPTER = new HibernateJpaVendorAdapter();

    @Bean
    public HibernateJpaVendorAdapter hibernateJpaVendorAdapter() {
        HIBERNATE_JPA_VENDOR_ADAPTER.setDatabasePlatform("org.hibernate.dialect.MySQL8Dialect");
        return HIBERNATE_JPA_VENDOR_ADAPTER;
    }
}*/
